<UserControl x:Class="RSS_Module.View.RssReaderTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RSS_Module.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10">
        <TextBox x:Name="txtUrl" Text="{Binding RSSUrl}" Grid.Row="0" Grid.Column="0" Width="130" Height="20"   />
        <Button Content="Add Subscription" 
                            Grid.Row="0" 
                            VerticalAlignment="Top"
                            Height="25"  
                            Grid.Column="0" 
                            Margin="10"
                            Width="Auto" 
                            Background="#FF919137" 
                            FontSize="14" 
                            Foreground="#FF000000" 
                            FontWeight="Bold" 
                            Command="{Binding AddSubscriptionCommand}"/>
    </StackPanel>
</UserControl>
